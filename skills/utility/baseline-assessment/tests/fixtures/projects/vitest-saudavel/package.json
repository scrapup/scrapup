{
  "name": "vitest-saudavel",
  "version": "1.0.0",
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "lint": "biome check .",
    "build": "tsup src/index.ts"
  },
  "devDependencies": {
    "vitest": "^1.0.0",
    "@biomejs/biome": "^1.5.0"
  }
}
